###### Core

- `JavaScript` + `TypeScript` ( `React.js` / `Node` / `jQuery` )
- C# `ASP.NET MVC`, `WebAPI`, `WCF`
- `CSS` (`SASS`)
- Agile dev lead
- Test Driven Development `TDD`

###### Front end

- `JavaScript` + `TypeScript`
- `React.js` / `redux` / `redux-thunk` / `lodash` / `date-fns`
- `SASS` / `CSS` / `styled-components` / `CSS-in-js`
- `Webpack` / `Rollup` / `Babel`
- `jest` / `enzyme` / `selenium` / `cypress`

###### Server

- C# expert: `MVC` / `Web API` / `.NET Core`
- `Node.js` / `Express`
- Theory and implementation of REST and SOA architectural patterns.

###### Database

- `ADO.NET` & `Entity Framework` ORM with `MSSQL`.
- `NHibernate`
- `Dapper ORM`

###### Mobile

- `React Native` experience

###### Other

- Linting and meta-programing - `tslint` / `eslint` / `roslyn` rule implementations
- `Powershell`
- `Bash`
- CI / CD tools : `Travis CI` / `TeamCity` / `Octopus`
